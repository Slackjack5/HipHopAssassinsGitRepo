using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using EZCameraShake;

public class BeatFX : MonoBehaviour
{

    // Update is called once per frame
    void Update()
    {
        
    }

  public void BumpOnBeat()
  {
    //CameraShaker.Instance.ShakeOnce(2, 2f, .1f, .1f);
  }
}
